# On-Call Ticket Monitor Environment Variables

# Flask Secret Key (generate a random key for production)
SECRET_KEY=your-secret-key-change-this-in-production

# Note: API keys are now managed through the web interface
# in the Settings page. This is the recommended approach.

# Legacy API key configuration (only used as fallback)
# ATERA_API_KEY=your-atera-api-key
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=your-twilio-phone-number
